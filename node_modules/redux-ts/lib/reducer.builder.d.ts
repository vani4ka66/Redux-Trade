import { Action, ActionBody, ActionCreatorDefinition } from './';
export declare class ReducerBuilder<State = {}> {
    private actions;
    private initState;
    /**
     * Initial state of the reducer
     *
     * @param {State} state State object
     * @returns
     * @memberof ReducerBuilder
     */
    init(state: State): this;
    /**
     * Consumer definition for given action type
     *
     * @template TPayload Action payload type
     * @param {ActionCreatorDefinition<TPayload>} creator Action creator function
     * @param {ActionBody<State, Action<TPayload>>} body Action body
     * @returns
     * @memberof ReducerBuilder
     */
    handle<TPayload, TMeta>(creator: ActionCreatorDefinition<TPayload, TMeta>, body: ActionBody<State, Action<TPayload, TMeta>>): this;
    private build;
}
